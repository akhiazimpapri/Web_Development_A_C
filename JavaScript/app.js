//arrow function
const sum = (a,b) => { 
    console.log(a+b);
}

const cube = (a) => {
    return a * a * a; 
}

//it can be writen as like this when a simple work is done.
const mul = (a,b) => (
    a * b
);